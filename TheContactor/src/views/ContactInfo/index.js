import React from "react";
import {View, Text, Image} from 'react-native';

const ContactInfo = ({navigation: { navigate } }) => {

    return (
        <View>
            <Text>contact info</Text>
        </View>
    );
};

export default ContactInfo
